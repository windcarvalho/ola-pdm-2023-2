import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Olá PDM 2024',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
